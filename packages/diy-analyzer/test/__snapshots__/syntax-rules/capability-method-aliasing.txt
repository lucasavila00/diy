packages/alpha/src/sample.ts:15:8 unused capability
  13 |
  14 |
> 15 | export function bad(capabilities: Capabilities<FsCapability>): void {
     |        ^ Declares unused capability "core.fs".
  16 | 	const direct = capabilities.need;
  17 | 	const { provide: provideCapabilities, need, override } = capabilities;
  18 | 	let rebound = direct;
packages/alpha/src/sample.ts:30:8 unused capability
  28 | }
  29 |
> 30 | export function returns(capabilities: Capabilities<FsCapability>) {
     |        ^ Declares unused capability "core.fs".
  31 | 	return capabilities.override;
  32 | }
  33 |
packages/alpha/src/sample.ts:16:17 aliased capability method
  14 |
  15 | export function bad(capabilities: Capabilities<FsCapability>): void {
> 16 | 	const direct = capabilities.need;
     | 	               ^ Do not alias, rebind, return, or pass capability methods; call them directly.
  17 | 	const { provide: provideCapabilities, need, override } = capabilities;
  18 | 	let rebound = direct;
  19 | 	rebound = capabilities.need;
packages/alpha/src/sample.ts:16:17 escaped capabilities
  14 |
  15 | export function bad(capabilities: Capabilities<FsCapability>): void {
> 16 | 	const direct = capabilities.need;
     | 	               ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  17 | 	const { provide: provideCapabilities, need, override } = capabilities;
  18 | 	let rebound = direct;
  19 | 	rebound = capabilities.need;
packages/alpha/src/sample.ts:17:10 aliased capability method
  15 | export function bad(capabilities: Capabilities<FsCapability>): void {
  16 | 	const direct = capabilities.need;
> 17 | 	const { provide: provideCapabilities, need, override } = capabilities;
     | 	        ^ Do not alias, rebind, return, or pass capability methods; call them directly.
  18 | 	let rebound = direct;
  19 | 	rebound = capabilities.need;
  20 | 	rebound = capabilities.provide;
packages/alpha/src/sample.ts:17:40 aliased capability method
  15 | export function bad(capabilities: Capabilities<FsCapability>): void {
  16 | 	const direct = capabilities.need;
> 17 | 	const { provide: provideCapabilities, need, override } = capabilities;
     | 	                                      ^ Do not alias, rebind, return, or pass capability methods; call them directly.
  18 | 	let rebound = direct;
  19 | 	rebound = capabilities.need;
  20 | 	rebound = capabilities.provide;
packages/alpha/src/sample.ts:17:46 aliased capability method
  15 | export function bad(capabilities: Capabilities<FsCapability>): void {
  16 | 	const direct = capabilities.need;
> 17 | 	const { provide: provideCapabilities, need, override } = capabilities;
     | 	                                            ^ Do not alias, rebind, return, or pass capability methods; call them directly.
  18 | 	let rebound = direct;
  19 | 	rebound = capabilities.need;
  20 | 	rebound = capabilities.provide;
packages/alpha/src/sample.ts:17:59 escaped capabilities
  15 | export function bad(capabilities: Capabilities<FsCapability>): void {
  16 | 	const direct = capabilities.need;
> 17 | 	const { provide: provideCapabilities, need, override } = capabilities;
     | 	                                                         ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  18 | 	let rebound = direct;
  19 | 	rebound = capabilities.need;
  20 | 	rebound = capabilities.provide;
packages/alpha/src/sample.ts:19:12 aliased capability method
  17 | 	const { provide: provideCapabilities, need, override } = capabilities;
  18 | 	let rebound = direct;
> 19 | 	rebound = capabilities.need;
     | 	          ^ Do not alias, rebind, return, or pass capability methods; call them directly.
  20 | 	rebound = capabilities.provide;
  21 | 	rebound = capabilities.override;
  22 | 	use(capabilities.need);
packages/alpha/src/sample.ts:19:12 escaped capabilities
  17 | 	const { provide: provideCapabilities, need, override } = capabilities;
  18 | 	let rebound = direct;
> 19 | 	rebound = capabilities.need;
     | 	          ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  20 | 	rebound = capabilities.provide;
  21 | 	rebound = capabilities.override;
  22 | 	use(capabilities.need);
packages/alpha/src/sample.ts:20:12 aliased capability method
  18 | 	let rebound = direct;
  19 | 	rebound = capabilities.need;
> 20 | 	rebound = capabilities.provide;
     | 	          ^ Do not alias, rebind, return, or pass capability methods; call them directly.
  21 | 	rebound = capabilities.override;
  22 | 	use(capabilities.need);
  23 | 	use(capabilities.provide);
packages/alpha/src/sample.ts:20:12 escaped capabilities
  18 | 	let rebound = direct;
  19 | 	rebound = capabilities.need;
> 20 | 	rebound = capabilities.provide;
     | 	          ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  21 | 	rebound = capabilities.override;
  22 | 	use(capabilities.need);
  23 | 	use(capabilities.provide);
packages/alpha/src/sample.ts:21:12 aliased capability method
  19 | 	rebound = capabilities.need;
  20 | 	rebound = capabilities.provide;
> 21 | 	rebound = capabilities.override;
     | 	          ^ Do not alias, rebind, return, or pass capability methods; call them directly.
  22 | 	use(capabilities.need);
  23 | 	use(capabilities.provide);
  24 | 	use(capabilities.override);
packages/alpha/src/sample.ts:21:12 escaped capabilities
  19 | 	rebound = capabilities.need;
  20 | 	rebound = capabilities.provide;
> 21 | 	rebound = capabilities.override;
     | 	          ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  22 | 	use(capabilities.need);
  23 | 	use(capabilities.provide);
  24 | 	use(capabilities.override);
packages/alpha/src/sample.ts:22:6 aliased capability method
  20 | 	rebound = capabilities.provide;
  21 | 	rebound = capabilities.override;
> 22 | 	use(capabilities.need);
     | 	    ^ Do not alias, rebind, return, or pass capability methods; call them directly.
  23 | 	use(capabilities.provide);
  24 | 	use(capabilities.override);
  25 | 	use(provideCapabilities);
packages/alpha/src/sample.ts:22:6 escaped capabilities
  20 | 	rebound = capabilities.provide;
  21 | 	rebound = capabilities.override;
> 22 | 	use(capabilities.need);
     | 	    ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  23 | 	use(capabilities.provide);
  24 | 	use(capabilities.override);
  25 | 	use(provideCapabilities);
packages/alpha/src/sample.ts:23:6 aliased capability method
  21 | 	rebound = capabilities.override;
  22 | 	use(capabilities.need);
> 23 | 	use(capabilities.provide);
     | 	    ^ Do not alias, rebind, return, or pass capability methods; call them directly.
  24 | 	use(capabilities.override);
  25 | 	use(provideCapabilities);
  26 | 	use(need);
packages/alpha/src/sample.ts:23:6 escaped capabilities
  21 | 	rebound = capabilities.override;
  22 | 	use(capabilities.need);
> 23 | 	use(capabilities.provide);
     | 	    ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  24 | 	use(capabilities.override);
  25 | 	use(provideCapabilities);
  26 | 	use(need);
packages/alpha/src/sample.ts:24:6 aliased capability method
  22 | 	use(capabilities.need);
  23 | 	use(capabilities.provide);
> 24 | 	use(capabilities.override);
     | 	    ^ Do not alias, rebind, return, or pass capability methods; call them directly.
  25 | 	use(provideCapabilities);
  26 | 	use(need);
  27 | 	use(override);
packages/alpha/src/sample.ts:24:6 escaped capabilities
  22 | 	use(capabilities.need);
  23 | 	use(capabilities.provide);
> 24 | 	use(capabilities.override);
     | 	    ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  25 | 	use(provideCapabilities);
  26 | 	use(need);
  27 | 	use(override);
packages/alpha/src/sample.ts:31:9 aliased capability method
  29 |
  30 | export function returns(capabilities: Capabilities<FsCapability>) {
> 31 | 	return capabilities.override;
     | 	       ^ Do not alias, rebind, return, or pass capability methods; call them directly.
  32 | }
  33 |
packages/alpha/src/sample.ts:31:9 escaped capabilities
  29 |
  30 | export function returns(capabilities: Capabilities<FsCapability>) {
> 31 | 	return capabilities.override;
     | 	       ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  32 | }
  33 |
