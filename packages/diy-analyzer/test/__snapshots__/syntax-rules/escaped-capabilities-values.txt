packages/alpha/src/sample.ts:20:8 unused capability
  18 | declare const ServiceBox: { new (capabilities: Capabilities<FsCapability>): unknown };
  19 |
> 20 | export function bad(capabilities: Capabilities<FsCapability>, input: string): unknown {
     |        ^ Declares unused capability "core.fs".
  21 | 	const local = capabilities;
  22 | 	const box = { capabilities };
  23 | 	const list = [capabilities];
packages/alpha/src/sample.ts:21:16 escaped capabilities
  19 |
  20 | export function bad(capabilities: Capabilities<FsCapability>, input: string): unknown {
> 21 | 	const local = capabilities;
     | 	              ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  22 | 	const box = { capabilities };
  23 | 	const list = [capabilities];
  24 | 	moduleCapabilities = capabilities;
packages/alpha/src/sample.ts:22:16 escaped capabilities
  20 | export function bad(capabilities: Capabilities<FsCapability>, input: string): unknown {
  21 | 	const local = capabilities;
> 22 | 	const box = { capabilities };
     | 	              ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  23 | 	const list = [capabilities];
  24 | 	moduleCapabilities = capabilities;
  25 | 	child(input, capabilities);
packages/alpha/src/sample.ts:23:16 escaped capabilities
  21 | 	const local = capabilities;
  22 | 	const box = { capabilities };
> 23 | 	const list = [capabilities];
     | 	              ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  24 | 	moduleCapabilities = capabilities;
  25 | 	child(input, capabilities);
  26 | 	use(new ServiceBox(capabilities));
packages/alpha/src/sample.ts:24:23 escaped capabilities
  22 | 	const box = { capabilities };
  23 | 	const list = [capabilities];
> 24 | 	moduleCapabilities = capabilities;
     | 	                     ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  25 | 	child(input, capabilities);
  26 | 	use(new ServiceBox(capabilities));
  27 | 	use(capabilities.logger);
packages/alpha/src/sample.ts:25:15 escaped capabilities
  23 | 	const list = [capabilities];
  24 | 	moduleCapabilities = capabilities;
> 25 | 	child(input, capabilities);
     | 	             ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  26 | 	use(new ServiceBox(capabilities));
  27 | 	use(capabilities.logger);
  28 | 	use(local);
packages/alpha/src/sample.ts:26:21 escaped capabilities
  24 | 	moduleCapabilities = capabilities;
  25 | 	child(input, capabilities);
> 26 | 	use(new ServiceBox(capabilities));
     | 	                   ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  27 | 	use(capabilities.logger);
  28 | 	use(local);
  29 | 	use(box);
packages/alpha/src/sample.ts:27:6 escaped capabilities
  25 | 	child(input, capabilities);
  26 | 	use(new ServiceBox(capabilities));
> 27 | 	use(capabilities.logger);
     | 	    ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  28 | 	use(local);
  29 | 	use(box);
  30 | 	use(list);
packages/alpha/src/sample.ts:32:9 escaped capabilities
  30 | 	use(list);
  31 | 	use(moduleCapabilities);
> 32 | 	return capabilities;
     | 	       ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  33 | }
  34 |
