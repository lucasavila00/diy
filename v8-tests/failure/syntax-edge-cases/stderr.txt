src/main.ts:12:8 unused capability
  10 | void diy;
  11 |
> 12 | export function destructureRest(capabilities: Capabilities<ReadCapability>): void {
     |        ^ Declares unused capability "read".
  13 | 	const { ...rest } = capabilities;
  14 | 	use(rest);
  15 | }
  = help: remove "read" from `Capabilities<...>`, or add a real `capabilities.read` read if it is required
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:17:8 unused capability
  15 | }
  16 |
> 17 | export function destructureService(capabilities: Capabilities<ReadCapability>): void {
     |        ^ Declares unused capability "read".
  18 | 	const { read } = capabilities;
  19 | 	use(read);
  20 | }
  = help: remove "read" from `Capabilities<...>`, or add a real `capabilities.read` read if it is required
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:22:8 unused capability
  20 | }
  21 |
> 22 | export function destructureComputedService(capabilities: Capabilities<ReadCapability>): void {
     |        ^ Declares unused capability "read".
  23 | 	const { ["read"]: read } = capabilities;
  24 | 	use(read);
  25 | }
  = help: remove "read" from `Capabilities<...>`, or add a real `capabilities.read` read if it is required
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:27:8 unused capability
  25 | }
  26 |
> 27 | export function destructureLiteralService(capabilities: Capabilities<ReadIdCapability>): void {
     |        ^ Declares unused capability "read-id".
  28 | 	const { "read-id": readId } = capabilities;
  29 | 	use(readId);
  30 | }
  = help: remove "read-id" from `Capabilities<...>`, or add a real `capabilities["read-id"]` read if it is required
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:2:8 renamed diy import
  1 | import type { Capabilities, Capability } from "@beff/diy";
> 2 | import * as diy from "@beff/diy";
    |        ^ Do not rename imports from @beff/diy; import exported names directly.
  3 |
  4 | type ReadCapability = Capability<"read", unknown>;
  5 | type ReadIdCapability = Capability<"read-id", unknown>;
  = help: use `import type { Capabilities, Capability } from "@beff/diy"`
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:13:8 aliased capability service
  11 |
  12 | export function destructureRest(capabilities: Capabilities<ReadCapability>): void {
> 13 | 	const { ...rest } = capabilities;
     | 	      ^ Do not create local aliases for capability services.
  14 | 	use(rest);
  15 | }
  16 |
  = help: read services inline from `capabilities`, for example `capabilities.reader`
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:18:8 aliased capability service
  16 |
  17 | export function destructureService(capabilities: Capabilities<ReadCapability>): void {
> 18 | 	const { read } = capabilities;
     | 	      ^ Do not create local aliases for capability services.
  19 | 	use(read);
  20 | }
  21 |
  = help: read services inline from `capabilities`, for example `capabilities.reader`
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:23:8 aliased capability service
  21 |
  22 | export function destructureComputedService(capabilities: Capabilities<ReadCapability>): void {
> 23 | 	const { ["read"]: read } = capabilities;
     | 	      ^ Do not create local aliases for capability services.
  24 | 	use(read);
  25 | }
  26 |
  = help: read services inline from `capabilities`, for example `capabilities.reader`
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:28:8 aliased capability service
  26 |
  27 | export function destructureLiteralService(capabilities: Capabilities<ReadIdCapability>): void {
> 28 | 	const { "read-id": readId } = capabilities;
     | 	      ^ Do not create local aliases for capability services.
  29 | 	use(readId);
  30 | }
  31 |
  = help: read services inline from `capabilities`, for example `capabilities.reader`
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:34:3 aliased capability service
  32 | export function assignDestructuredService(capabilities: Capabilities<ReadCapability>): void {
  33 | 	let read: unknown;
> 34 | 	({ read } = capabilities);
     | 	 ^ Do not create local aliases for capability services.
  35 | 	capabilities.read;
  36 | 	use(read);
  37 | }
  = help: read services inline from `capabilities`, for example `capabilities.reader`
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:40:20 escaped capabilities
  38 |
  39 | export function duplicateEscape(capabilities: Capabilities<ReadCapability>): void {
> 40 | 	use(capabilities, capabilities);
     | 	                  ^ Use `capabilities` only for static service property access or as the first argument to another effectful function.
  41 | }
  42 |
  43 | export function nestedShadow(capabilities: Capabilities<ReadCapability>): void {
  = help: keep capability flow statically analyzable: read services inline or forward `capabilities` as the first argument to a named effectful function
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:44:17 invalid capabilities parameter
  42 |
  43 | export function nestedShadow(capabilities: Capabilities<ReadCapability>): void {
> 44 | 	function inner(capabilities: unknown): void {
     | 	               ^ `capabilities` parameters must be typed as `Capabilities<...>`.
  45 | 		use(capabilities);
  46 | 	}
  47 | 	inner(capabilities);
  = help: write the signature as `function run(capabilities: Capabilities<AppCapability>, ...)`
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:52:2 invalid capabilities parameter
  50 | export function destructuredParam(
  51 | 	{ value }: { value: unknown },
> 52 | 	capabilities: Capabilities<ReadCapability>,
     | 	^ Capabilities parameters must be the first parameter.
  53 | ): void {
  54 | 	const { need } = other;
  55 | 	use(value);
  = help: write the signature as `function run(capabilities: Capabilities<AppCapability>, ...)`
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:60:1 re-exported diy capabilities
  58 | }
  59 |
> 60 | export * from "@beff/diy/capabilities";
     | ^ Do not re-export Capabilities from @beff/diy; import it directly where it is used.
  61 | export * as DiyCapabilities from "@beff/diy";
  62 | export type { Capability } from "@beff/diy";
  63 |
  = help: use `import type { Capabilities } from "@beff/diy"` in each module that needs it
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:61:1 re-exported diy capabilities
  59 |
  60 | export * from "@beff/diy/capabilities";
> 61 | export * as DiyCapabilities from "@beff/diy";
     | ^ Do not re-export Capabilities from @beff/diy; import it directly where it is used.
  62 | export type { Capability } from "@beff/diy";
  63 |
  = help: use `import type { Capabilities } from "@beff/diy"` in each module that needs it
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:39:8 unsupported analysis
  37 | }
  38 |
> 39 | export function duplicateEscape(capabilities: Capabilities<ReadCapability>): void {
     |        ^ unresolved capabilities forwarding target use
  40 | 	use(capabilities, capabilities);
  41 | }
  42 |
  = help: make the callee a local or imported function with `capabilities: Capabilities<...>` as its first parameter
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:43:8 unsupported analysis
  41 | }
  42 |
> 43 | export function nestedShadow(capabilities: Capabilities<ReadCapability>): void {
     |        ^ unresolved capabilities forwarding target inner
  44 | 	function inner(capabilities: unknown): void {
  45 | 		use(capabilities);
  46 | 	}
  = help: make the callee a local or imported function with `capabilities: Capabilities<...>` as its first parameter
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line
