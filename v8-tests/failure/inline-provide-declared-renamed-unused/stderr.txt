src/main.ts:12:8 unused capability
  10 | }
  11 |
> 12 | export function load(capabilities: Capabilities<LegacyClockCapability | AppClockCapability>): Date {
     |        ^ Declares unused capability "appClock".
  13 | 	return readRenamed(
  14 | 		Capabilities.extend(
  15 | 			capabilities,
  = help: remove "appClock" from `Capabilities<...>`, or add a real `capabilities.appClock` read if it is required
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line
src/main.ts:14:3 redundant capability provider
  12 | export function load(capabilities: Capabilities<LegacyClockCapability | AppClockCapability>): Date {
  13 | 	return readRenamed(
> 14 | 		Capabilities.extend(
     | 		^ Capabilities.extend adds capabilities already present on capabilities: appClock
  15 | 			capabilities,
  16 | 			Capabilities.create<AppClockCapability>({ appClock: capabilities.legacyClock }),
  17 | 		),
  = note: `appClock` is already allowed by this function's `capabilities` parameter
  = help: use `Capabilities.override(...)` when replacing an existing capability is intentional
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line
