src/main.ts:11:24 dynamic capability access
   9 |
  10 | 	// @ts-expect-error intentionally invalid for analyzer coverage
> 11 | 	use(capabilities.need(id));
     | 	                      ^ Capability IDs must be string literals or resolvable const string identifiers.
  12 | 	// @ts-expect-error intentionally invalid for analyzer coverage
  13 | 	use(capabilities.need(`core.${id}`));
  14 | 	// @ts-expect-error intentionally invalid for analyzer coverage
  = help: use a literal capability ID or a const string identifier, for example `capabilities.need("core.fs")`
src/main.ts:13:24 dynamic capability access
  11 | 	use(capabilities.need(id));
  12 | 	// @ts-expect-error intentionally invalid for analyzer coverage
> 13 | 	use(capabilities.need(`core.${id}`));
     | 	                      ^ Capability IDs must be string literals or resolvable const string identifiers.
  14 | 	// @ts-expect-error intentionally invalid for analyzer coverage
  15 | 	use(capabilities.need(dynamicId));
  16 | 	use(capabilities["need"]("core.fs"));
  = help: use a literal capability ID or a const string identifier, for example `capabilities.need("core.fs")`
src/main.ts:15:24 dynamic capability access
  13 | 	use(capabilities.need(`core.${id}`));
  14 | 	// @ts-expect-error intentionally invalid for analyzer coverage
> 15 | 	use(capabilities.need(dynamicId));
     | 	                      ^ Capability IDs must be string literals or resolvable const string identifiers.
  16 | 	use(capabilities["need"]("core.fs"));
  17 | 	use(capabilities["provide"]({ "core.clock": {} }));
  18 | 	use(capabilities.override?.({ "core.fs": {} }));
  = help: use a literal capability ID or a const string identifier, for example `capabilities.need("core.fs")`
src/main.ts:16:6 dynamic capability access
  14 | 	// @ts-expect-error intentionally invalid for analyzer coverage
  15 | 	use(capabilities.need(dynamicId));
> 16 | 	use(capabilities["need"]("core.fs"));
     | 	    ^ Use direct capability method calls like `capabilities.need(...)`, `capabilities.provide(...)`, or `capabilities.override(...)`.
  17 | 	use(capabilities["provide"]({ "core.clock": {} }));
  18 | 	use(capabilities.override?.({ "core.fs": {} }));
  19 | }
  = help: replace computed or optional access with direct property access, for example `capabilities.need("core.fs")`
src/main.ts:16:6 escaped capabilities
  14 | 	// @ts-expect-error intentionally invalid for analyzer coverage
  15 | 	use(capabilities.need(dynamicId));
> 16 | 	use(capabilities["need"]("core.fs"));
     | 	    ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  17 | 	use(capabilities["provide"]({ "core.clock": {} }));
  18 | 	use(capabilities.override?.({ "core.fs": {} }));
  19 | }
  = help: keep capability flow statically analyzable: call methods directly, or forward `capabilities` as the first argument to a named effectful function
src/main.ts:17:6 dynamic capability access
  15 | 	use(capabilities.need(dynamicId));
  16 | 	use(capabilities["need"]("core.fs"));
> 17 | 	use(capabilities["provide"]({ "core.clock": {} }));
     | 	    ^ Use direct capability method calls like `capabilities.need(...)`, `capabilities.provide(...)`, or `capabilities.override(...)`.
  18 | 	use(capabilities.override?.({ "core.fs": {} }));
  19 | }
  20 |
  = help: replace computed or optional access with direct property access, for example `capabilities.need("core.fs")`
src/main.ts:17:6 escaped capabilities
  15 | 	use(capabilities.need(dynamicId));
  16 | 	use(capabilities["need"]("core.fs"));
> 17 | 	use(capabilities["provide"]({ "core.clock": {} }));
     | 	    ^ Use `capabilities` only for direct capability method calls or as the first argument to another effectful function.
  18 | 	use(capabilities.override?.({ "core.fs": {} }));
  19 | }
  20 |
  = help: keep capability flow statically analyzable: call methods directly, or forward `capabilities` as the first argument to a named effectful function
src/main.ts:18:6 dynamic capability access
  16 | 	use(capabilities["need"]("core.fs"));
  17 | 	use(capabilities["provide"]({ "core.clock": {} }));
> 18 | 	use(capabilities.override?.({ "core.fs": {} }));
     | 	    ^ Use direct capability method calls like `capabilities.need(...)`, `capabilities.provide(...)`, or `capabilities.override(...)`.
  19 | }
  20 |
  = help: replace computed or optional access with direct property access, for example `capabilities.need("core.fs")`
