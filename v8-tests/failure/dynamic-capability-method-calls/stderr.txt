src/main.ts:11:6 dynamic capability access
   9 |
  10 | 	// @ts-expect-error intentionally invalid for analyzer coverage
> 11 | 	use(capabilities[id]);
     | 	    ^ Capability service access must use a static property name.
  12 | 	// @ts-expect-error intentionally invalid for analyzer coverage
  13 | 	use(capabilities[`fs${id}`]);
  14 | 	// @ts-expect-error intentionally invalid for analyzer coverage
  = help: use direct property access like `capabilities.reader`, or bracket access with a const string key
src/main.ts:13:6 dynamic capability access
  11 | 	use(capabilities[id]);
  12 | 	// @ts-expect-error intentionally invalid for analyzer coverage
> 13 | 	use(capabilities[`fs${id}`]);
     | 	    ^ Capability service access must use a static property name.
  14 | 	// @ts-expect-error intentionally invalid for analyzer coverage
  15 | 	use(capabilities[dynamicId]);
  16 | 	use(capabilities?.fs);
  = help: use direct property access like `capabilities.reader`, or bracket access with a const string key
src/main.ts:15:6 dynamic capability access
  13 | 	use(capabilities[`fs${id}`]);
  14 | 	// @ts-expect-error intentionally invalid for analyzer coverage
> 15 | 	use(capabilities[dynamicId]);
     | 	    ^ Capability service access must use a static property name.
  16 | 	use(capabilities?.fs);
  17 | 	// @ts-expect-error intentionally invalid for analyzer coverage
  18 | 	const { [dynamicId]: fs } = capabilities;
  = help: use direct property access like `capabilities.reader`, or bracket access with a const string key
src/main.ts:16:6 dynamic capability access
  14 | 	// @ts-expect-error intentionally invalid for analyzer coverage
  15 | 	use(capabilities[dynamicId]);
> 16 | 	use(capabilities?.fs);
     | 	    ^ Capability service access must use a static property name.
  17 | 	// @ts-expect-error intentionally invalid for analyzer coverage
  18 | 	const { [dynamicId]: fs } = capabilities;
  19 | 	use(fs);
  = help: use direct property access like `capabilities.reader`, or bracket access with a const string key
src/main.ts:18:10 dynamic capability access
  16 | 	use(capabilities?.fs);
  17 | 	// @ts-expect-error intentionally invalid for analyzer coverage
> 18 | 	const { [dynamicId]: fs } = capabilities;
     | 	        ^ Capability destructuring must use a static property name.
  19 | 	use(fs);
  20 | 	// @ts-expect-error intentionally invalid for analyzer coverage
  21 | 	const { [`fs${id}`]: templatedFs } = capabilities;
  = help: use direct property names like `{ reader }`, or computed const string keys
src/main.ts:21:10 dynamic capability access
  19 | 	use(fs);
  20 | 	// @ts-expect-error intentionally invalid for analyzer coverage
> 21 | 	const { [`fs${id}`]: templatedFs } = capabilities;
     | 	        ^ Capability destructuring must use a static property name.
  22 | 	use(templatedFs);
  23 | }
  24 |
  = help: use direct property names like `{ reader }`, or computed const string keys
src/main.ts:30:7 dynamic capability access
  28 | 	check(capabilities: Capabilities<FsCapability>): void {
  29 | 		// @ts-expect-error intentionally invalid for analyzer coverage
> 30 | 		use(capabilities.#fs);
     | 		    ^ Capability service access must use a static property name.
  31 | 	}
  32 | }
  33 |
  = help: use direct property access like `capabilities.reader`, or bracket access with a const string key
