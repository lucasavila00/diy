src/main.ts:13:3 redundant capability provider
  11 | export function load(capabilities: Capabilities<AppClockCapability>): Date {
  12 | 	return readRenamed(
> 13 | 		Capabilities.extend(
     | 		^ Capabilities.extend adds capabilities already present on capabilities: appClock
  14 | 			capabilities,
  15 | 			Capabilities.create<AppClockCapability>({ appClock: capabilities.appClock }),
  16 | 		),
  = note: `appClock` is already allowed by this function's `capabilities` parameter
  = help: use `Capabilities.override(...)` when replacing an existing capability is intentional
