src/main.ts:9:17 escaped capabilities
   7 | export function bad(capabilities: Capabilities<FsCapability>): void {
   8 | 	const fs = capabilities["core.fs"];
>  9 | 	const direct = capabilities;
     | 	               ^ Use `capabilities` only for static service property access, destructuring, or as the first argument to another effectful function.
  10 | 	let rebound: unknown = direct;
  11 | 	rebound = capabilities;
  12 | 	use(fs);
  = help: keep capability flow statically analyzable: read services directly, destructure static service names, or forward `capabilities` as the first argument to a named effectful function
src/main.ts:11:12 escaped capabilities
   9 | 	const direct = capabilities;
  10 | 	let rebound: unknown = direct;
> 11 | 	rebound = capabilities;
     | 	          ^ Use `capabilities` only for static service property access, destructuring, or as the first argument to another effectful function.
  12 | 	use(fs);
  13 | 	use(direct);
  14 | 	use(rebound);
  = help: keep capability flow statically analyzable: read services directly, destructure static service names, or forward `capabilities` as the first argument to a named effectful function
src/main.ts:19:9 escaped capabilities
  17 | export function returns(capabilities: Capabilities<FsCapability>) {
  18 | 	capabilities["core.fs"];
> 19 | 	return capabilities;
     | 	       ^ Use `capabilities` only for static service property access, destructuring, or as the first argument to another effectful function.
  20 | }
  21 |
  = help: keep capability flow statically analyzable: read services directly, destructure static service names, or forward `capabilities` as the first argument to a named effectful function
