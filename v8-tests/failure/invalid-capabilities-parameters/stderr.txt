src/main.ts:5:20 invalid capabilities alias
  3 | type FsCapability = Capability<"fs", unknown>;
  4 | type StorageCapability = Capability<"storage", unknown>;
> 5 | type StorageCaps = Capabilities<StorageCapability>;
    |                    ^ Do not alias DIY `Capabilities<...>`; write `Capabilities<...>` directly on the capabilities parameter.
  6 |
  7 | declare const defaultCapabilities: Capabilities<FsCapability>;
  8 | declare function use(value: unknown): void;
  = help: write `capabilities: Capabilities<AppCapability>` instead of an alias
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:10:25 invalid capabilities parameter
   8 | declare function use(value: unknown): void;
   9 |
> 10 | export function renamed(svc: Capabilities<FsCapability>): void {
     |                         ^ Capabilities parameters must be named `capabilities` or `_capabilities`.
  11 | 	use(svc);
  12 | }
  13 |
  = help: write the signature as `function run(capabilities: Capabilities<AppCapability>, ...)`
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:14:38 invalid capabilities parameter
  12 | }
  13 |
> 14 | export function second(path: string, capabilities: Capabilities<FsCapability>): void {
     |                                      ^ Capabilities parameters must be the first parameter.
  15 | 	use(path);
  16 | 	use(capabilities);
  17 | }
  = help: write the signature as `function run(capabilities: Capabilities<AppCapability>, ...)`
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:19:25 invalid capabilities parameter
  17 | }
  18 |
> 19 | export function aliased(capabilities: StorageCaps): void {
     |                         ^ `capabilities` parameters with a type annotation must use DIY `Capabilities<...>`.
  20 | 	use(capabilities);
  21 | }
  22 |
  = help: write the signature as `function run(capabilities: Capabilities<AppCapability>, ...)`
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line

src/main.ts:27:25 invalid capabilities parameter
  25 | }
  26 |
> 27 | export function untyped(capabilities: unknown): void {
     |                         ^ `capabilities` parameters with a type annotation must use DIY `Capabilities<...>`.
  28 | 	use(capabilities);
  29 | }
  30 |
  = help: write the signature as `function run(capabilities: Capabilities<AppCapability>, ...)`
  = help: for known analyzer false positives, add `// diy-ignore-next-line -- reason` on the previous line
